#include <stdio.h>
#include <stdlib.h>
#define max 4 

int stack_arr[max];
int top = - 1;

void push(int data)
{
    if (top == max - 1){
        printf("stack overfloewe");
        return ;
    }

    top = top+ 1;
    stack_arr[top]= data ;}
    
    
    int pop(){
        int value ;
       value=stack_arr[top];
       top = -1;
       return 0 ;
    }
    
    
    int main()
    {
        int data ;
        push(1);
        push(2);
        push(3);
       push(3);
       data= pop();
        return 0 ;
        
    }


